export const CARD_DEFS:any = {
  taglio_fiammante:{id:'taglio_fiammante',name:'Taglio Fiammante',color:'red',cost:0,speed:'response',type:'Magia',text:'Durante un combattimento, infliggi 1 danno al Campione attaccante.',effect:'taglio'},
  sfera_incandescente:{id:'sfera_incandescente',name:'Sfera Incandescente',color:'red',cost:0,speed:'base',type:'Magia',text:'Infliggi 1 danno a un nemico e 1 danno a un tuo Campione.',effect:'sfera'},
  corazza_esplosiva:{id:'corazza_esplosiva',name:'Corazza Esplosiva',color:'red',cost:0,speed:'instant',type:'Magia',text:'Infliggi 1 danno a un tuo Campione. Poi ottiene +1 POW fino alla fine del turno.',effect:'corazza'},
  occhio_di_drago:{id:'occhio_di_drago',name:'Occhio di Drago',color:'red',cost:0,speed:'response',type:'Magia',text:'Metti le prime 3 carte del tuo mazzo nel Cimitero. Un tuo Campione ottiene +2 POW fino alla fine del turno.',effect:'occhio'},
  mano_del_caos:{id:'mano_del_caos',name:'Mano del Caos',color:'red',cost:1,speed:'base',type:'Magia',text:'Scarta la tua mano. Un tuo Campione ottiene +1 POW per ogni carta scartata fino alla fine del turno.',effect:'mano'},
  nube_di_fuoco:{id:'nube_di_fuoco',name:'Nube di Fuoco',color:'red',cost:1,speed:'base',type:'Magia',text:'Fino alla fine del turno, le tue Magie infliggono 1 danno aggiuntivo.',effect:'nube'},
  tornado_bollente:{id:'tornado_bollente',name:'Tornado Bollente',color:'red',cost:1,speed:'base',type:'Magia',text:'Infliggi 1 danno a ogni Campione e a ogni Mostro.',effect:'tornado'},
  fendente_di_fuoco:{id:'fendente_di_fuoco',name:'Fendente di Fuoco',color:'red',cost:1,speed:'base',type:'Magia',text:'Infliggi 3 danni a un nemico.',effect:'fendente'},
  berserk:{id:'berserk',name:'Berserk',color:'red',cost:2,speed:'instant',type:'Magia',text:'Infliggi 1 danno a un tuo Campione. Se quel danno gli infligge una Ferita, ottiene +5 POW e diventa attivo.',effect:'berserk'},
  stupido:{id:'stupido',name:'Stupido',color:'green',cost:0,speed:'instant',type:'Magia',text:'Infliggi 1 danno a un Mostro.',effect:'stupido'},
  riflesso:{id:'riflesso',name:'Riflesso',color:'green',cost:0,speed:'base',type:'Magia',text:'Scegli un tuo Campione. Infliggi a un Mostro danni pari ai danni attualmente subiti da quel Campione.',effect:'riflesso'},
  tutto_per_la_festa:{id:'tutto_per_la_festa',name:'Tutto per la Festa',color:'green',cost:0,speed:'base',type:'Magia',text:'Guarda le prime 3 carte del tuo Monster Deck. Puoi scartarne quante ne vuoi. Rimetti le altre in cima nell’ordine che preferisci.',effect:'festa'},
  taglio_ninjitsu:{id:'taglio_ninjitsu',name:'Tecnica del Taglio Ninjitsu',color:'green',cost:0,speed:'base',type:'Magia',text:'Infliggi 2 danni a un Mostro.',effect:'taglio_ninjitsu'},
  doppia_katana:{id:'doppia_katana',name:'Doppia Katana',color:'green',cost:1,speed:'base',type:'Magia',text:'Attiva un tuo Campione.',effect:'katana'},
  alta_marea:{id:'alta_marea',name:'Alta Marea',color:'green',cost:1,speed:'instant',type:'Magia',text:'Scegli un tuo Campione in combattimento. Annulla il combattimento che lo coinvolge.',effect:'marea'},
  albero_della_vita:{id:'albero_della_vita',name:'Albero della Vita',color:'green',cost:1,speed:'instant',type:'Supporto',text:'Un tuo Campione ottiene +2 POW fino alla fine del turno.',effect:'albero'},
  sguardo_ninjitsu:{id:'sguardo_ninjitsu',name:'Tecnica dello Sguardo Ninjitsu',color:'green',cost:1,speed:'instant',type:'Magia',text:'Uccidi un Mostro.',effect:'sguardo'},
  mille_lame:{id:'mille_lame',name:'Tecnica delle Mille Lame Ninjitsu',color:'green',cost:12,speed:'instant',type:'Magia',text:'Costa 1 Anima Verde in meno per ogni Mostro nel tuo Cimitero. Un tuo Campione ottiene +4 POW fino alla fine del turno.',effect:'mille'},
};

export const MONSTER_DEFS:any = {
  lucertola_fuoco:{id:'lucertola_fuoco',name:'Lucertola di fuoco',color:'red',pow:3,text:''},
  segugio_infernale:{id:'segugio_infernale',name:'Segugio Infernale',color:'red',pow:3,text:'Quando questo Mostro entra in gioco, infligge 1 danno a ogni Mostro adiacente.'},
  fenice_cremisi:{id:'fenice_cremisi',name:'Fenice Cremisi',color:'red',pow:3,text:'Puoi pagare 1 Anima per giocare questo Mostro dal Cimitero.'},
  golem_magmatico:{id:'golem_magmatico',name:'Golem Magmatico',color:'red',pow:3,text:'La prima Magia che lancia ogni giocatore ogni turno infligge 1 danno aggiuntivo.'},
  drago_delle_ceneri:{id:'drago_delle_ceneri',name:'Drago delle Ceneri',color:'red',pow:4,text:'Quando entra in gioco, infligge 1 danno a tutti gli altri Mostri.'},
  salamandra_vulcanica:{id:'salamandra_vulcanica',name:'Salamandra Vulcanica',color:'red',pow:5,text:'Ogni volta che un Campione attacca, perde 1 POW fino alla fine del turno.'},
  ragno_dei_germogli:{id:'ragno_dei_germogli',name:'Ragno dei Germogli',color:'green',pow:2,text:'Finché questo Mostro è in gioco, le Magie costano 1 Anima in più.'},
  serpente_della_giungla:{id:'serpente_della_giungla',name:'Serpente della Giungla',color:'green',pow:3,text:'Quando questo Mostro va nel Cimitero, ogni giocatore perde 1 Anima.'},
  lupo_delle_radici:{id:'lupo_delle_radici',name:'Lupo delle Radici',color:'green',pow:3,text:'Tutti gli altri Mostri hanno +1 POW.'},
  cervo_antico:{id:'cervo_antico',name:'Cervo Antico',color:'green',pow:3,text:'Quando un Mostro subisce 1 danno, ottiene +1 POW per questo turno.'},
  guardiano_della_foresta:{id:'guardiano_della_foresta',name:'Guardiano della Foresta',color:'green',pow:4,text:'Finché questo Mostro è in gioco, gli altri Mostri non possono essere scelti come bersaglio.'},
  orso_furioso:{id:'orso_furioso',name:'Orso Furioso',color:'green',pow:2,text:'Ogni volta che muore un altro Mostro, questo ottiene +2 POW fino alla fine del turno.'},
};

export const STARTER_DECK=Object.keys(CARD_DEFS);
export const STARTER_MONSTERS=Object.keys(MONSTER_DEFS);
const clone=(x:any)=>JSON.parse(JSON.stringify(x));
export const other=(p:number)=>p===1?2:1;
export const shuffle=(arr:any[])=>{const a=[...arr];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]];}return a};
function player(s:any,p:number){return s.players[String(p)]}
function champById(s:any,p:number,id:string){return player(s,p)?.champions.find((c:any)=>c.id===id)}
function monsterByUid(s:any,uid:string){return s.board.monsters.find((m:any)=>m.uid===uid)}
function monsterCount(s:any,id:string){return s.board.monsters.filter((m:any)=>m.cardId===id).length}
function log(s:any,msg:string){s.log.push(msg);if(s.log.length>120)s.log=s.log.slice(-120)}

export function newPlayer(name:string){return{name,souls:{red:3,green:3},recycleCount:0,champions:[{id:'kael',name:'Kael Infuocato',color:'red',basePow:3,hp:3,wounds:0,damage:0,tempPow:0,tapped:false,defeated:false},{id:'lyrandel',name:'Lyrandel Spirito della Natura',color:'green',basePow:3,hp:3,wounds:0,damage:0,tempPow:0,tapped:false,defeated:false}],deck:shuffle(STARTER_DECK),grave:[],hand:[],selected:false,monsterDeck:shuffle(STARTER_MONSTERS),monsterGrave:[],fireCloud:false,lyrandelUsed:false,firstMagicCast:false}}
export function newState(name:string){return{status:'waiting',turn:1,focus:1,priority:null,mainPasses:0,priorityPasses:0,stack:[],stackInitiator:null,combat:null,pendingChoice:null,winner:null,players:{'1':newPlayer(name),'2':null},board:{monsters:[]},log:[`Partita creata da ${name}.`]}}
export function currentPow(s:any,p:number,c:any){let v=c.basePow+(c.tempPow||0);if(c.id==='kael'&&player(s,p).hand.length===0&&s.status==='main')v+=3;return Math.max(0,v)}
export function currentMonsterPow(s:any,m:any){const d=MONSTER_DEFS[m.cardId];if(!d)return 0;let v=d.pow+(m.tempPow||0);v+=s.board.monsters.filter((x:any)=>x.cardId==='lupo_delle_radici'&&x.uid!==m.uid).length;return Math.max(0,v)}
function guardedMonster(s:any){return s.board.monsters.some((m:any)=>m.cardId==='guardiano_della_foresta')}
function canTargetMonster(s:any,uid:string){const m=monsterByUid(s,uid);return !!m&&(!guardedMonster(s)||m.cardId==='guardiano_della_foresta')}
function dynamicCost(s:any,p:number,c:any){let cost=c.id==='mille_lame'?Math.max(0,12-player(s,p).monsterGrave.length):c.cost;if(c.type==='Magia')cost+=monsterCount(s,'ragno_dei_germogli');return Math.max(0,cost)}
function canPay(s:any,p:number,c:any){return player(s,p).souls[c.color]>=dynamicCost(s,p,c)}
function pay(s:any,p:number,c:any){const cost=dynamicCost(s,p,c);player(s,p).souls[c.color]-=cost;return cost}
function spellDamage(s:any,p:number,n:number,item:any){return n+(player(s,p).fireCloud?1:0)+Number(item?.golemBoost||0)}
function checkWinner(s:any){for(const p of[1,2]){const pl=player(s,p);if(pl&&pl.champions.every((c:any)=>c.defeated)){s.status='gameover';s.winner=other(p);log(s,`${player(s,other(p)).name} vince la partita!`)}}}
function woundChampion(s:any,p:number,c:any,source=''){c.wounds+=1;c.damage=0;log(s,`${c.name} subisce una Ferita (${c.wounds}/${c.hp})${source?` da ${source}`:''}.`);if(c.wounds>=c.hp){c.defeated=true;c.tapped=true;log(s,`${c.name} è sconfitto.`);checkWinner(s)}}
export function damageChampion(s:any,p:number,id:string,n:number,source=''){const c=champById(s,p,id);if(!c||c.defeated||n<=0)return{wounded:false};c.damage+=n;log(s,`${c.name} subisce ${n} dann${n===1?'o':'i'} (${c.damage}/${currentPow(s,p,c)}).`);if(c.damage>=currentPow(s,p,c)){woundChampion(s,p,c,source);return{wounded:true}}return{wounded:false}}
function gainSoul(s:any,p:number,color:string){if(p!==1&&p!==2)return;const pl=player(s,p),before=pl.souls[color]||0;pl.souls[color]=Math.min(3,before+1);if(pl.souls[color]>before)log(s,`${pl.name} recupera 1 Anima ${color==='red'?'Rossa':'Verde'}.`)}
function loseSoul(s:any,p:number){const pl=player(s,p);if(!pl)return;if(pl.souls.green>0){pl.souls.green--;log(s,`${pl.name} perde 1 Anima Verde.`)}else if(pl.souls.red>0){pl.souls.red--;log(s,`${pl.name} perde 1 Anima Rossa.`)}}
function sweepDead(s:any,killer:number|null,reason=''){let again=true;while(again){again=false;for(const x of[...s.board.monsters]){const m=monsterByUid(s,x.uid);if(m&&m.damage>=currentMonsterPow(s,m)){killMonster(s,killer,m,reason,true);again=true;break}}}}
export function killMonster(s:any,killer:number|null,m:any,reason='',skip=false){const i=s.board.monsters.findIndex((x:any)=>x.uid===m.uid);if(i<0)return;s.board.monsters.splice(i,1);player(s,m.owner)?.monsterGrave.push(m.cardId);const d=MONSTER_DEFS[m.cardId];log(s,`${d?.name||m.cardId} viene sconfitto${reason?` da ${reason}`:''}.`);if(killer===1||killer===2)gainSoul(s,killer,d.color);for(const b of s.board.monsters.filter((x:any)=>x.cardId==='orso_furioso')){b.tempPow=(b.tempPow||0)+2;log(s,'Orso Furioso ottiene +2 POW fino alla fine del turno.')}if(m.cardId==='serpente_della_giungla')for(const p of[1,2])if(player(s,p))loseSoul(s,p);if(!skip)sweepDead(s,killer,reason)}
function recordLyrandelDamage(s:any,p:number|null,uid:string,allow:boolean){if(!allow||(p!==1&&p!==2))return;const w=s._lyrandelWindow;if(w&&w.player===p&&!w.uids.includes(uid))w.uids.push(uid)}
function applyMonsterDamage(s:any,m:any,n:number){if(!m||n<=0)return false;m.damage+=n;if(n===1){const deer=monsterCount(s,'cervo_antico');if(deer){m.tempPow=(m.tempPow||0)+deer;log(s,`${MONSTER_DEFS[m.cardId].name} ottiene +${deer} POW dal Cervo Antico fino alla fine del turno.`)}}log(s,`${MONSTER_DEFS[m.cardId].name} subisce ${n} dann${n===1?'o':'i'} (${m.damage}/${currentMonsterPow(s,m)}).`);return m.damage>=currentMonsterPow(s,m)}
export function damageMonster(s:any,p:number|null,uid:string,n:number,source='',allowLyrandel=true){let m=monsterByUid(s,uid);if(!m||n<=0)return;recordLyrandelDamage(s,p,uid,allowLyrandel);if(applyMonsterDamage(s,m,n)){killMonster(s,p,m,source);return}}
function beginLyrandelWindow(s:any,p:number){const pl=player(s,p),lyr=champById(s,p,'lyrandel');delete s._lyrandelWindow;if(pl&&lyr&&!lyr.defeated&&!pl.lyrandelUsed)s._lyrandelWindow={player:p,uids:[]}}
function endLyrandelWindow(s:any,resume:string,initiator?:number){const w=s._lyrandelWindow;delete s._lyrandelWindow;if(!w||!w.uids.length)return false;const pl=player(s,w.player),lyr=champById(s,w.player,'lyrandel');if(!pl||!lyr||lyr.defeated||pl.lyrandelUsed)return false;pl.lyrandelUsed=true;const targets=[...new Set(w.uids)].filter((uid:string)=>!!monsterByUid(s,uid));if(!targets.length){log(s,'Lyrandel si attiva, ma nessuno dei Mostri danneggiati è ancora in gioco.');return false}s.pendingChoice={type:'lyrandel',player:w.player,monsterUids:targets,resume,initiator};log(s,`${lyr.name} si attiva: scegli uno dei Mostri appena danneggiati.`);return true}
function resolveMonsterEnter(s:any,m:any){if(!monsterByUid(s,m.uid))return;if(m.cardId==='segugio_infernale'){const i=s.board.monsters.findIndex((x:any)=>x.uid===m.uid),ids=[s.board.monsters[i-1]?.uid,s.board.monsters[i+1]?.uid].filter(Boolean);for(const id of ids)if(monsterByUid(s,id))damageMonster(s,null,id,1,'Segugio Infernale',false)}else if(m.cardId==='drago_delle_ceneri'){for(const id of s.board.monsters.filter((x:any)=>x.uid!==m.uid).map((x:any)=>x.uid))if(monsterByUid(s,id))damageMonster(s,null,id,1,'Drago delle Ceneri',false)}}
function addMonstersForTurn(s:any){const entered:string[]=[];for(const p of[1,2]){const pl=player(s,p);for(let i=0;i<2&&pl.monsterDeck.length;i++){const cardId=pl.monsterDeck.shift(),m={uid:crypto.randomUUID(),cardId,owner:p,damage:0,tempPow:0};s.board.monsters.push(m);entered.push(m.uid);log(s,`${pl.name} rivela ${MONSTER_DEFS[cardId].name}.`)}}for(const uid of entered){const m=monsterByUid(s,uid);if(m)resolveMonsterEnter(s,m)}}
function startMainIfReady(s:any){if(player(s,1)?.selected&&player(s,2)?.selected){addMonstersForTurn(s);s.status='main';s.focus=s.turn%2?1:2;s.priority=null;s.mainPasses=0;s.priorityPasses=0;s.stack=[];s.combat=null;s.stackInitiator=null;log(s,`Inizia il turno ${s.turn}. Il Focus è di ${player(s,s.focus).name}.`)}}
function endTurn(s:any){for(const p of[1,2]){const pl=player(s,p);for(const c of pl.champions){c.damage=0;c.tempPow=0;if(!c.defeated)c.tapped=false}pl.fireCloud=false;pl.lyrandelUsed=false;pl.firstMagicCast=false;pl.deck=shuffle([...pl.deck,...pl.hand]);pl.hand=[];pl.selected=false}for(const m of s.board.monsters){m.damage=0;m.tempPow=0}s.turn++;s.status='select';s.focus=s.turn%2?1:2;s.priority=null;s.mainPasses=0;s.priorityPasses=0;s.stack=[];s.combat=null;s.pendingChoice=null;log(s,`Fine turno. Scegliete 6 carte per il turno ${s.turn}.`)}
function finishAction(s:any,initiator:number){s.priority=null;s.priorityPasses=0;s.stack=[];s.stackInitiator=null;s.combat=null;s.mainPasses=0;s.focus=other(initiator)}
function resolveCombat(s:any){const c=s.combat;if(!c)return;const atk=champById(s,c.attacker.player,c.attacker.champId);if(!atk||atk.defeated||c.cancelled){log(s,'Il combattimento viene annullato.');return}const n=currentPow(s,c.attacker.player,atk);if(c.target.type==='champion'){const t=champById(s,c.target.player,c.target.champId);if(t&&!t.defeated){log(s,`${atk.name} attacca ${t.name} con ${n} POW.`);damageChampion(s,c.target.player,t.id,n,atk.name)}else log(s,'Il bersaglio del combattimento non è più valido.')}else{const m=monsterByUid(s,c.target.uid);if(m){log(s,`${atk.name} attacca ${MONSTER_DEFS[m.cardId].name} con ${n} POW.`);damageMonster(s,c.attacker.player,m.uid,n,atk.name,true)}else log(s,'Il Mostro bersaglio non è più sul campo: l’attacco fallisce.')}}
function validateTargetEnemy(s:any,p:number,t:any){if(t?.type==='champion')return t.player===other(p)&&!!champById(s,t.player,t.champId)&&!champById(s,t.player,t.champId).defeated;if(t?.type==='monster')return canTargetMonster(s,t.uid);return false}
function resolveSpell(s:any,item:any){const p=item.actor,pl=player(s,p),c=CARD_DEFS[item.cardId],t=item.targets||{};log(s,`Si risolve ${c.name}.`);switch(c.effect){case'taglio':if(s.combat){const a=s.combat.attacker;damageChampion(s,a.player,a.champId,spellDamage(s,p,1,item),c.name)}break;case'sfera':{const d=spellDamage(s,p,1,item);if(t.enemy?.type==='champion')damageChampion(s,t.enemy.player,t.enemy.champId,d,c.name);else if(t.enemy?.type==='monster')damageMonster(s,p,t.enemy.uid,d,c.name,true);damageChampion(s,p,t.ownChamp,d,c.name);break}case'corazza':{damageChampion(s,p,t.ownChamp,spellDamage(s,p,1,item),c.name);const ch=champById(s,p,t.ownChamp);if(ch&&!ch.defeated){ch.tempPow++;log(s,`${ch.name} ottiene +1 POW fino alla fine del turno.`)}break}case'occhio':if(pl.deck.length>=3){pl.grave.push(...pl.deck.splice(0,3));const ch=champById(s,p,t.ownChamp);if(ch&&!ch.defeated){ch.tempPow+=2;log(s,`${pl.name} manda 3 carte nel Cimitero. ${ch.name} ottiene +2 POW.`)}}break;case'mano':{const n=pl.hand.length;pl.grave.push(...pl.hand);pl.hand=[];const ch=champById(s,p,t.ownChamp);if(ch&&!ch.defeated){ch.tempPow+=n;log(s,`${pl.name} scarta ${n} carte. ${ch.name} ottiene +${n} POW.`)}break}case'nube':pl.fireCloud=true;log(s,`Le Magie di ${pl.name} infliggono +1 danno fino alla fine del turno.`);break;case'tornado':{const d=spellDamage(s,p,1,item);for(const q of[1,2])for(const ch of player(s,q).champions.filter((x:any)=>!x.defeated))damageChampion(s,q,ch.id,d,c.name);for(const m of[...s.board.monsters])if(monsterByUid(s,m.uid))damageMonster(s,p,m.uid,d,c.name,true);break}case'fendente':{const d=spellDamage(s,p,3,item);if(t.enemy?.type==='champion')damageChampion(s,t.enemy.player,t.enemy.champId,d,c.name);else if(t.enemy?.type==='monster')damageMonster(s,p,t.enemy.uid,d,c.name,true);break}case'berserk':{const r=damageChampion(s,p,t.ownChamp,spellDamage(s,p,1,item),c.name),ch=champById(s,p,t.ownChamp);if(r.wounded&&ch&&!ch.defeated){ch.tempPow+=5;ch.tapped=false;log(s,`${ch.name} entra in Berserk: +5 POW e diventa attivo.`)}break}case'stupido':damageMonster(s,p,t.monsterUid,spellDamage(s,p,1,item),c.name,true);break;case'riflesso':{const ch=champById(s,p,t.ownChamp);if(ch&&!ch.defeated)damageMonster(s,p,t.monsterUid,spellDamage(s,p,ch.damage,item),c.name,true);break}case'festa':{const top=pl.monsterDeck.slice(0,3);s.pendingChoice={type:'festa',player:p,top};log(s,`${pl.name} guarda le prime ${top.length} carte del proprio Monster Deck.`);return'pause'}case'taglio_ninjitsu':damageMonster(s,p,t.monsterUid,spellDamage(s,p,2,item),c.name,true);break;case'katana':{const ch=champById(s,p,t.ownChamp);if(ch&&!ch.defeated){ch.tapped=false;log(s,`${ch.name} diventa attivo.`)}break}case'marea':if(s.combat){const involved=(s.combat.attacker.player===p&&s.combat.attacker.champId===t.ownChamp)||(s.combat.target.type==='champion'&&s.combat.target.player===p&&s.combat.target.champId===t.ownChamp);if(involved){s.combat.cancelled=true;log(s,'Alta Marea annulla il combattimento.')}}break;case'albero':{const ch=champById(s,p,t.ownChamp);if(ch&&!ch.defeated){ch.tempPow+=2;log(s,`${ch.name} ottiene +2 POW fino alla fine del turno.`)}break}case'sguardo':{const m=monsterByUid(s,t.monsterUid);if(m)killMonster(s,p,m,c.name);break}case'mille':{const ch=champById(s,p,t.ownChamp);if(ch&&!ch.defeated){ch.tempPow+=4;log(s,`${ch.name} ottiene +4 POW fino alla fine del turno.`)}break}}return'ok'}
function continueResolution(s:any){while(s.stack.length){const item=s.stack.pop();beginLyrandelWindow(s,item.actor);const r=resolveSpell(s,item);player(s,item.actor).grave.push(item.cardId);if(r==='pause')return;if(endLyrandelWindow(s,'continue'))return}const initiator=s.stackInitiator??s.combat?.initiator??s.focus;if(s.combat){beginLyrandelWindow(s,s.combat.attacker.player);resolveCombat(s);if(endLyrandelWindow(s,'finish_combat',initiator))return}finishAction(s,initiator)}

export function act(s:any,p:number,a:any){
 if(s.status==='gameover')throw new Error('La partita è terminata.');
 if(s.pendingChoice){
  if(a.type!=='resolve_choice'||s.pendingChoice.player!==p)throw new Error('C’è una scelta in attesa.');
  if(s.pendingChoice.type==='festa'){const pl=player(s,p),top=s.pendingChoice.top,discard=(a.discard||[]).filter((id:string)=>top.includes(id)),remain=top.filter((id:string)=>!discard.includes(id)),order=a.order||[];if(order.length!==remain.length||order.some((id:string)=>!remain.includes(id)))throw new Error('Ordine non valido.');pl.monsterDeck=[...order,...pl.monsterDeck.slice(top.length)];pl.monsterGrave.push(...discard);s.pendingChoice=null;log(s,`${pl.name} scarta ${discard.length} Mostr${discard.length===1?'o':'i'} e riordina gli altri.`);continueResolution(s);return s}
  if(s.pendingChoice.type==='lyrandel'){const pc=s.pendingChoice,uid=String(a.monsterUid||'');if(!pc.monsterUids.includes(uid)||!monsterByUid(s,uid))throw new Error('Bersaglio di Lyrandel non valido.');s.pendingChoice=null;const m=monsterByUid(s,uid);log(s,`Lyrandel infligge 1 danno aggiuntivo a ${MONSTER_DEFS[m.cardId].name}.`);damageMonster(s,p,uid,1,'Lyrandel',false);if(pc.resume==='continue')continueResolution(s);else if(pc.resume==='finish_combat')finishAction(s,pc.initiator??s.focus);return s}
 }
 if(a.type==='select_cards'){if(s.status!=='select')throw new Error('Non è il momento di scegliere le carte.');const pl=player(s,p);if(pl.selected)throw new Error('Hai già confermato.');const ids=[...new Set(a.cardIds||[])];if(ids.length!==6||ids.some((id:string)=>!pl.deck.includes(id)))throw new Error('Devi scegliere esattamente 6 carte disponibili.');pl.hand=ids;pl.deck=pl.deck.filter((id:string)=>!ids.includes(id));pl.selected=true;log(s,`${pl.name} ha scelto le 6 carte.`);startMainIfReady(s);return s}
 if(s.status!=='main')throw new Error('La fase principale non è attiva.');
 if(a.type==='attack'){if(s.priority||s.stack.length||s.combat)throw new Error('Non puoi attaccare durante una Catena.');if(s.focus!==p)throw new Error('Non hai il Focus.');const c=champById(s,p,a.champId);if(!c||c.defeated||c.tapped)throw new Error('Campione non disponibile.');if(!validateTargetEnemy(s,p,a.target))throw new Error('Bersaglio non valido.');const sal=monsterCount(s,'salamandra_vulcanica');if(sal){c.tempPow=(c.tempPow||0)-sal;log(s,`${c.name} perde ${sal} POW per la Salamandra Vulcanica fino alla fine del turno.`);if(c.damage>=currentPow(s,p,c)&&!c.defeated)woundChampion(s,p,c,'Salamandra Vulcanica')}if(c.defeated)throw new Error('Il Campione viene sconfitto prima di poter attaccare.');c.tapped=true;s.combat={initiator:p,attacker:{player:p,champId:c.id},target:a.target,cancelled:false};s.priority=other(p);s.priorityPasses=0;s.mainPasses=0;log(s,`${player(s,p).name} dichiara un attacco con ${c.name}.`);return s}
 if(a.type==='cast'){const pl=player(s,p),c=CARD_DEFS[a.cardId];if(!c||!pl.hand.includes(c.id))throw new Error('Carta non disponibile in mano.');if(s.stack.length){if(s.priority!==p)throw new Error('Non hai priorità nella Catena.');if(c.speed!=='instant')throw new Error('Durante una Catena puoi giocare solo Istantanee.')}else if(s.combat){if(s.priority!==p)throw new Error('Non hai priorità nel combattimento.');if(c.speed!=='response')throw new Error('La prima carta durante un combattimento deve essere una Risposta.')}else if(s.focus!==p)throw new Error('Non hai il Focus.');if(c.effect==='taglio'&&!s.combat)throw new Error('Taglio Fiammante richiede un Campione attaccante.');if(c.effect==='marea'&&!s.combat)throw new Error('Alta Marea richiede un combattimento.');if(c.effect==='occhio'&&pl.deck.length<3)throw new Error('Non hai 3 carte nel mazzo da scartare.');if(['stupido','taglio_ninjitsu','sguardo','riflesso'].includes(c.effect)&&!canTargetMonster(s,a.targets?.monsterUid))throw new Error('Bersaglio Mostro non valido.');if(['sfera','fendente'].includes(c.effect)&&a.targets?.enemy?.type==='monster'&&!canTargetMonster(s,a.targets.enemy.uid))throw new Error('Bersaglio Mostro non valido.');if(!canPay(s,p,c))throw new Error('Anime insufficienti.');const cost=pay(s,p,c);pl.hand=pl.hand.filter((id:string)=>id!==c.id);let golemBoost=0;if(c.type==='Magia'&&!pl.firstMagicCast){pl.firstMagicCast=true;golemBoost=monsterCount(s,'golem_magmatico')}s.stack.push({actor:p,cardId:c.id,targets:a.targets||{},golemBoost});if(s.stack.length===1)s.stackInitiator=s.combat?.initiator??p;s.mainPasses=0;s.priorityPasses=0;s.priority=other(p);log(s,`${pl.name} gioca ${c.name}${cost?` pagando ${cost} Anima${cost===1?'':'e'} ${c.color==='red'?'Rossa':'Verde'}`:''}.`);return s}
 if(a.type==='revive_phoenix'){if(s.priority||s.stack.length||s.combat||s.focus!==p)throw new Error('Puoi rianimare la Fenice solo con il Focus e senza Catene.');const pl=player(s,p),i=pl.monsterGrave.indexOf('fenice_cremisi'),color=a.color==='red'?'red':a.color==='green'?'green':null;if(i<0)throw new Error('Fenice Cremisi non è nel tuo Cimitero Mostri.');if(!color||pl.souls[color]<1)throw new Error('Devi pagare 1 Anima disponibile.');pl.souls[color]--;pl.monsterGrave.splice(i,1);const m={uid:crypto.randomUUID(),cardId:'fenice_cremisi',owner:p,damage:0,tempPow:0};s.board.monsters.push(m);log(s,`${pl.name} paga 1 Anima ${color==='red'?'Rossa':'Verde'} e gioca Fenice Cremisi dal Cimitero.`);resolveMonsterEnter(s,m);s.focus=other(p);s.mainPasses=0;return s}
 if(a.type==='pass_priority'){if(s.priority!==p)throw new Error('Non hai priorità.');s.priorityPasses++;if(s.priorityPasses>=2){s.priorityPasses=0;if(s.stack.length)continueResolution(s);else if(s.combat){const init=s.combat.initiator;beginLyrandelWindow(s,s.combat.attacker.player);resolveCombat(s);if(!endLyrandelWindow(s,'finish_combat',init))finishAction(s,init)}}else s.priority=other(p);return s}
 if(a.type==='pass'){if(s.priority||s.stack.length||s.combat)throw new Error('Usa Passa priorità durante combattimenti o Catene.');if(s.focus!==p)throw new Error('Non hai il Focus.');s.mainPasses++;log(s,`${player(s,p).name} passa.`);if(s.mainPasses>=2)endTurn(s);else s.focus=other(p);return s}
 if(a.type==='recycle'){if(s.priority||s.stack.length||s.combat||s.focus!==p)throw new Error('Puoi Riciclare solo con il Focus e senza Catene.');const pl=player(s,p),cost=pl.recycleCount+1,r=Number(a.red||0),g=Number(a.green||0);if(r+g!==cost||r<0||g<0||r>pl.souls.red||g>pl.souls.green)throw new Error('Pagamento del Riciclo non valido.');if(!pl.grave.length)throw new Error('Il Cimitero è vuoto.');pl.souls.red-=r;pl.souls.green-=g;pl.deck=shuffle([...pl.deck,...pl.grave]);pl.grave=[];pl.recycleCount++;log(s,`${pl.name} Ricicla il Cimitero pagando ${cost} Anime.`);s.focus=other(p);s.mainPasses=0;return s}
 throw new Error('Azione sconosciuta.');
}

export function publicView(state:any,p:number){const s=clone(state);delete s._lyrandelWindow;for(const q of[1,2]){const src=player(state,q),pl=s.players[String(q)];if(!pl)continue;pl.champions=pl.champions.map((c:any)=>({...c,pow:currentPow(state,q,src.champions.find((x:any)=>x.id===c.id))}));pl.graveCards=src.grave.map((id:string)=>CARD_DEFS[id]);pl.monsterGraveCards=src.monsterGrave.map((id:string)=>MONSTER_DEFS[id]);if(q!==p){pl.handCount=src.hand.length;delete pl.hand;pl.deckCount=src.deck.length;delete pl.deck;pl.graveCount=src.grave.length;delete pl.grave;pl.monsterDeckCount=src.monsterDeck.length;delete pl.monsterDeck;pl.monsterGraveCount=src.monsterGrave.length;delete pl.monsterGrave}else{pl.handCards=src.hand.map((id:string)=>({...CARD_DEFS[id],effectiveCost:dynamicCost(state,q,CARD_DEFS[id])}));pl.deckCards=src.deck.map((id:string)=>CARD_DEFS[id])}}s.board.monsters=s.board.monsters.map((m:any)=>({...m,...MONSTER_DEFS[m.cardId],pow:currentMonsterPow(state,m)}));s.cardDefs=CARD_DEFS;s.monsterDefs=MONSTER_DEFS;s.you=p;if(s.pendingChoice&&s.pendingChoice.player!==p)s.pendingChoice={type:s.pendingChoice.type,player:s.pendingChoice.player,hidden:true};return s}
