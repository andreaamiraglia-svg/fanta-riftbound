export const CARD_DEFS:any = {
  taglio_fiammante:{id:'taglio_fiammante',name:'Taglio Fiammante',color:'red',cost:0,speed:'response',type:'Magia',text:'Durante un combattimento, infliggi 1 danno al Campione attaccante.',effect:'taglio'},
  sfera_incandescente:{id:'sfera_incandescente',name:'Sfera Incandescente',color:'red',cost:0,speed:'base',type:'Magia',text:'Infliggi 1 danno a un nemico e 1 danno a un tuo Campione.',effect:'sfera'},
  corazza_esplosiva:{id:'corazza_esplosiva',name:'Corazza Esplosiva',color:'red',cost:0,speed:'instant',type:'Magia',text:'Infliggi 1 danno a un tuo Campione. Poi ottiene +1 POW fino alla fine del turno.',effect:'corazza'},
  occhio_di_drago:{id:'occhio_di_drago',name:'Occhio di Drago',color:'red',cost:0,speed:'response',type:'Magia',text:'Metti le prime 3 carte del tuo mazzo nel Cimitero. Un tuo Campione ottiene +2 POW fino alla fine del turno.',effect:'occhio'},
  mano_del_caos:{id:'mano_del_caos',name:'Mano del Caos',color:'red',cost:1,speed:'base',type:'Magia',text:'Scarta la tua mano. Un tuo Campione ottiene +1 POW per ogni carta scartata fino alla fine del turno.',effect:'mano'},
  nube_di_fuoco:{id:'nube_di_fuoco',name:'Nube di Fuoco',color:'red',cost:1,speed:'base',type:'Magia',text:'Fino alla fine del turno, le tue Magie infliggono 1 danno aggiuntivo.',effect:'nube'},
  tornado_bollente:{id:'tornado_bollente',name:'Tornado Bollente',color:'red',cost:1,speed:'base',type:'Magia',text:'Infliggi 1 danno a ogni Campione e a ogni Mostro.',effect:'tornado'},
  fendente_di_fuoco:{id:'fendente_di_fuoco',name:'Fendente di Fuoco',color:'red',cost:1,speed:'base',type:'Magia',text:'Infliggi 3 danni a un nemico.',effect:'fendente'},
  berserk:{id:'berserk',name:'Berserk',color:'red',cost:2,speed:'instant',type:'Magia',text:'Infliggi 1 danno a un tuo Campione. Se quel danno gli infligge una Ferita, ottiene +5 POW e diventa attivo.',effect:'berserk'},
  stupido:{id:'stupido',name:'Stupido',color:'green',cost:0,speed:'instant',type:'Magia',text:'Infliggi 1 danno a un Mostro.',effect:'stupido'},
  riflesso:{id:'riflesso',name:'Riflesso',color:'green',cost:0,speed:'base',type:'Magia',text:'Scegli un tuo Campione. Infliggi a un Mostro danni pari ai danni attualmente subiti da quel Campione.',effect:'riflesso'},
  tutto_per_la_festa:{id:'tutto_per_la_festa',name:'Tutto per la Festa',color:'green',cost:0,speed:'base',type:'Magia',text:'Guarda le prime 3 carte del tuo Monster Deck. Puoi scartarne quante ne vuoi. Rimetti le altre in cima nell’ordine che preferisci.',effect:'festa'},
  taglio_ninjitsu:{id:'taglio_ninjitsu',name:'Tecnica del Taglio Ninjitsu',color:'green',cost:0,speed:'base',type:'Magia',text:'Infliggi 2 danni a un Mostro.',effect:'taglio_ninjitsu'},
  doppia_katana:{id:'doppia_katana',name:'Doppia Katana',color:'green',cost:1,speed:'base',type:'Magia',text:'Attiva un tuo Campione.',effect:'katana'},
  alta_marea:{id:'alta_marea',name:'Alta Marea',color:'green',cost:1,speed:'instant',type:'Magia',text:'Scegli un tuo Campione in combattimento. Annulla il combattimento che lo coinvolge.',effect:'marea'},
  albero_della_vita:{id:'albero_della_vita',name:'Albero della Vita',color:'green',cost:1,speed:'instant',type:'Supporto',text:'Un tuo Campione ottiene +2 POW fino alla fine del turno.',effect:'albero'},
  sguardo_ninjitsu:{id:'sguardo_ninjitsu',name:'Tecnica dello Sguardo Ninjitsu',color:'green',cost:1,speed:'instant',type:'Magia',text:'Uccidi un Mostro.',effect:'sguardo'},
  mille_lame:{id:'mille_lame',name:'Tecnica delle Mille Lame Ninjitsu',color:'green',cost:12,speed:'instant',type:'Magia',text:'Costa 1 Anima Verde in meno per ogni Mostro nel tuo Cimitero. Un tuo Campione ottiene +4 POW fino alla fine del turno.',effect:'mille'},
};

export const MONSTER_DEFS:any = {
  lucertola_fuoco:{id:'lucertola_fuoco',name:'Lucertola di fuoco',color:'red',pow:3,text:''},
  segugio_infernale:{id:'segugio_infernale',name:'Segugio Infernale',color:'red',pow:3,text:'Quando questo Mostro entra in gioco, infligge 1 danno a ogni Mostro adiacente.'},
  fenice_cremisi:{id:'fenice_cremisi',name:'Fenice Cremisi',color:'red',pow:3,text:'Puoi pagare 1 Anima per giocare questo Mostro dal Cimitero.'},
  golem_magmatico:{id:'golem_magmatico',name:'Golem Magmatico',color:'red',pow:3,text:'La prima Magia che lancia ogni giocatore ogni turno infligge 1 danno aggiuntivo.'},
  drago_delle_ceneri:{id:'drago_delle_ceneri',name:'Drago delle Ceneri',color:'red',pow:4,text:'Quando entra in gioco, infligge 1 danno a tutti gli altri Mostri.'},
  salamandra_vulcanica:{id:'salamandra_vulcanica',name:'Salamandra Vulcanica',color:'red',pow:5,text:'Ogni volta che un Campione attacca, perde 1 POW fino alla fine del turno.'},
  ragno_dei_germogli:{id:'ragno_dei_germogli',name:'Ragno dei Germogli',color:'green',pow:2,text:'Finché questo Mostro è in gioco, le Magie costano 1 Anima in più.'},
  serpente_della_giungla:{id:'serpente_della_giungla',name:'Serpente della Giungla',color:'green',pow:3,text:'Quando questo Mostro va nel Cimitero, ogni giocatore perde 1 Anima.'},
  lupo_delle_radici:{id:'lupo_delle_radici',name:'Lupo delle Radici',color:'green',pow:3,text:'Tutti gli altri Mostri hanno +1 POW.'},
  cervo_antico:{id:'cervo_antico',name:'Cervo Antico',color:'green',pow:3,text:'Quando un Mostro subisce 1 danno, ottiene +1 POW per questo turno.'},
  guardiano_della_foresta:{id:'guardiano_della_foresta',name:'Guardiano della Foresta',color:'green',pow:4,text:'Finché questo Mostro è in gioco, gli altri Mostri non possono essere scelti come bersaglio.'},
  orso_furioso:{id:'orso_furioso',name:'Orso Furioso',color:'green',pow:2,text:'Ogni volta che muore un altro Mostro, questo ottiene +2 POW fino alla fine del turno.'},
  salamandra:{id:'salamandra',name:'Cucciolo di Salamandra',color:'red',pow:2,text:''},
  cinghiale_magmatico:{id:'cinghiale_magmatico',name:'Cinghiale Magmatico',color:'red',pow:4,text:''},
  serpente_cenere:{id:'serpente_cenere',name:'Serpente di Cenere',color:'red',pow:3,text:''},
  golem_lava:{id:'golem_lava',name:'Golem di Lava',color:'red',pow:5,text:''},
  falco_cremisi:{id:'falco_cremisi',name:'Falco Cremisi',color:'red',pow:2,text:''},
  lupo_selva:{id:'lupo_selva',name:'Lupo della Selva',color:'green',pow:3,text:''},
  orso_muschio:{id:'orso_muschio',name:'Orso di Muschio',color:'green',pow:4,text:''},
  serpente_rovi:{id:'serpente_rovi',name:'Serpente di Rovi',color:'green',pow:3,text:''},
  guardiano_arboreo:{id:'guardiano_arboreo',name:'Guardiano Arboreo',color:'green',pow:5,text:''},
  volpe_spirituale:{id:'volpe_spirituale',name:'Volpe Spirituale',color:'green',pow:2,text:''},
};

export const STARTER_DECK = Object.keys(CARD_DEFS);
export const STARTER_MONSTERS = ['lucertola_fuoco','segugio_infernale','fenice_cremisi','golem_magmatico','drago_delle_ceneri','salamandra_vulcanica','ragno_dei_germogli','serpente_della_giungla','lupo_delle_radici','cervo_antico','guardiano_della_foresta','orso_furioso'];
const clone=(x:any)=>JSON.parse(JSON.stringify(x));
export const other=(p:number)=>p===1?2:1;
export const shuffle=(arr:any[])=>{const a=[...arr];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]];}return a};
export function newPlayer(name:string){return {name,souls:{red:3,green:3},recycleCount:0,champions:[{id:'kael',name:'Kael Infuocato',color:'red',basePow:3,hp:3,wounds:0,damage:0,tempPow:0,tapped:false,defeated:false},{id:'lyrandel',name:'Lyrandel Spirito della Natura',color:'green',basePow:3,hp:3,wounds:0,damage:0,tempPow:0,tapped:false,defeated:false}],deck:shuffle(STARTER_DECK),grave:[],hand:[],selected:false,monsterDeck:shuffle(STARTER_MONSTERS),monsterGrave:[],fireCloud:false,lyrandelUsed:false,firstMagicCast:false}}
export function newState(p1Name:string){return {status:'waiting',turn:1,focus:1,priority:null,mainPasses:0,priorityPasses:0,stack:[],stackInitiator:null,combat:null,pendingChoice:null,winner:null,players:{'1':newPlayer(p1Name),'2':null},board:{monsters:[]},log:[`Partita creata da ${p1Name}.`]}}
function log(state:any,msg:string){state.log.push(msg);if(state.log.length>120)state.log=state.log.slice(-120)}
function player(state:any,p:number){return state.players[String(p)]}
function champById(state:any,p:number,id:string){return player(state,p).champions.find((c:any)=>c.id===id)}
function monsterByUid(state:any,uid:string){return state.board.monsters.find((m:any)=>m.uid===uid)}
function monsterCount(state:any,id:string){return state.board.monsters.filter((m:any)=>m.cardId===id).length}
export function currentPow(state:any,p:number,champ:any){let v=champ.basePow+(champ.tempPow||0);if(champ.id==='kael'&&player(state,p).hand.length===0&&state.status==='main')v+=3;return Math.max(0,v)}
export function currentMonsterPow(state:any,m:any){const def=MONSTER_DEFS[m.cardId];if(!def)return 0;let v=def.pow+(m.tempPow||0);v+=state.board.monsters.filter((x:any)=>x.cardId==='lupo_delle_radici'&&x.uid!==m.uid).length;return Math.max(0,v)}
function guardedMonster(state:any){return state.board.monsters.some((m:any)=>m.cardId==='guardiano_della_foresta')}
function canTargetMonster(state:any,uid:string){const m=monsterByUid(state,uid);if(!m)return false;if(!guardedMonster(state))return true;return m.cardId==='guardiano_della_foresta'}
function dynamicCost(state:any,p:number,card:any){let cost=card.id==='mille_lame'?Math.max(0,12-player(state,p).monsterGrave.length):card.cost;if(card.type==='Magia')cost+=monsterCount(state,'ragno_dei_germogli');return Math.max(0,cost)}
function canPay(state:any,p:number,card:any){const cost=dynamicCost(state,p,card);return player(state,p).souls[card.color]>=cost}
function pay(state:any,p:number,card:any){const cost=dynamicCost(state,p,card);player(state,p).souls[card.color]-=cost;return cost}
function spellDamage(state:any,p:number,amount:number,item:any){return amount+(player(state,p).fireCloud?1:0)+Number(item?.golemBoost||0)}
function checkWinner(state:any){for(const p of [1,2]){const pl=player(state,p);if(pl&&pl.champions.every((c:any)=>c.defeated)){state.status='gameover';state.winner=other(p);log(state,`${player(state,other(p)).name} vince la partita!`)}}}
function woundChampion(state:any,p:number,c:any,source:string){c.wounds+=1;c.damage=0;log(state,`${c.name} subisce una Ferita (${c.wounds}/${c.hp})${source?` da ${source}`:''}.`);if(c.wounds>=c.hp){c.defeated=true;c.tapped=true;log(state,`${c.name} è sconfitto.`);checkWinner(state)}}
export function damageChampion(state:any,p:number,champId:string,amount:number,source=''){const c=champById(state,p,champId);if(!c||c.defeated||amount<=0)return {wounded:false};c.damage+=amount;log(state,`${c.name} subisce ${amount} dann${amount===1?'o':'i'} (${c.damage}/${currentPow(state,p,c)}).`);if(c.damage>=currentPow(state,p,c)){woundChampion(state,p,c,source);return {wounded:true}}return {wounded:false}}
function gainSoul(state:any,p:number,color:string){if(p!==1&&p!==2)return;const pl=player(state,p);if(!(color in pl.souls))return;const before=pl.souls[color];pl.souls[color]=Math.min(3,pl.souls[color]+1);if(pl.souls[color]>before)log(state,`${pl.name} recupera 1 Anima ${color==='red'?'Rossa':'Verde'}.`)}
function loseOneSoul(state:any,p:number){const pl=player(state,p);if(!pl)return;if(pl.souls.green>0){pl.souls.green-=1;log(state,`${pl.name} perde 1 Anima Verde.`)}else if(pl.souls.red>0){pl.souls.red-=1;log(state,`${pl.name} perde 1 Anima Rossa.`)}}
function sweepDeadMonsters(state:any,killer:number|null,reason=''){let changed=true;while(changed){changed=false;for(const x of [...state.board.monsters]){const m=monsterByUid(state,x.uid);if(m&&m.damage>=currentMonsterPow(state,m)){killMonster(state,killer,m,reason,true);changed=true;break}}}}
export function killMonster(state:any,killer:number|null,m:any,reason='',skipSweep=false){const idx=state.board.monsters.findIndex((x:any)=>x.uid===m.uid);if(idx<0)return;state.board.monsters.splice(idx,1);const owner=player(state,m.owner);if(owner)owner.monsterGrave.push(m.cardId);const def=MONSTER_DEFS[m.cardId];log(state,`${def?.name||m.cardId} viene sconfitto${reason?` da ${reason}`:''}.`);if(killer===1||killer===2)gainSoul(state,killer,def?.color);for(const bear of state.board.monsters.filter((x:any)=>x.cardId==='orso_furioso')){bear.tempPow=(bear.tempPow||0)+2;log(state,'Orso Furioso ottiene +2 POW fino alla fine del turno.')}if(m.cardId==='serpente_della_giungla')for(const p of [1,2])if(player(state,p))loseOneSoul(state,p);if(!skipSweep)sweepDeadMonsters(state,killer,reason)}
function applyMonsterDamage(state:any,m:any,amount:number,source=''){if(!m||amount<=0)return false;m.damage+=amount;if(amount===1){const deer=monsterCount(state,'cervo_antico');if(deer>0){m.tempPow=(m.tempPow||0)+deer;log(state,`${MONSTER_DEFS[m.cardId].name} ottiene +${deer} POW dal Cervo Antico fino alla fine del turno.`)}}log(state,`${MONSTER_DEFS[m.cardId].name} subisce ${amount} dann${amount===1?'o':'i'} (${m.damage}/${currentMonsterPow(state,m)}).`);return m.damage>=currentMonsterPow(state,m)}
export function damageMonster(state:any,p:number|null,uid:string,amount:number,source='',allowLyrandel=true){let m=monsterByUid(state,uid);if(!m||amount<=0)return;if(applyMonsterDamage(state,m,amount,source)){killMonster(state,p,m,source);return}if(allowLyrandel&&(p===1||p===2)){const lyr=champById(state,p,'lyrandel'),pl=player(state,p);if(lyr&&!lyr.defeated&&!pl.lyrandelUsed){pl.lyrandelUsed=true;log(state,`Lyrandel aggiunge 1 danno a ${MONSTER_DEFS[m.cardId].name}.`);m=monsterByUid(state,uid);if(m&&applyMonsterDamage(state,m,1,'Lyrandel'))killMonster(state,p,m,'Lyrandel')}}}
function resolveMonsterEnter(state:any,m:any){if(!monsterByUid(state,m.uid))return;if(m.cardId==='segugio_infernale'){const idx=state.board.monsters.findIndex((x:any)=>x.uid===m.uid);const targets=[state.board.monsters[idx-1]?.uid,state.board.monsters[idx+1]?.uid].filter(Boolean);for(const uid of targets)if(monsterByUid(state,uid))damageMonster(state,null,uid,1,'Segugio Infernale',false)}else if(m.cardId==='drago_delle_ceneri'){const targets=state.board.monsters.filter((x:any)=>x.uid!==m.uid).map((x:any)=>x.uid);for(const uid of targets)if(monsterByUid(state,uid))damageMonster(state,null,uid,1,'Drago delle Ceneri',false)}}
function addMonstersForTurn(state:any){const entered:any[]=[];for(const p of [1,2]){const pl=player(state,p);for(let i=0;i<2&&pl.monsterDeck.length;i++){const cardId=pl.monsterDeck.shift();const m={uid:crypto.randomUUID(),cardId,owner:p,damage:0,tempPow:0};state.board.monsters.push(m);entered.push(m.uid);log(state,`${pl.name} rivela ${MONSTER_DEFS[cardId].name}.`)}}for(const uid of entered){const m=monsterByUid(state,uid);if(m)resolveMonsterEnter(state,m)}}
function startMainIfReady(state:any){if(state.players['1']?.selected&&state.players['2']?.selected){addMonstersForTurn(state);state.status='main';state.focus=state.turn%2===1?1:2;state.priority=null;state.mainPasses=0;state.priorityPasses=0;state.stack=[];state.combat=null;state.stackInitiator=null;log(state,`Inizia il turno ${state.turn}. Il Focus è di ${player(state,state.focus).name}.`)}}
function endTurn(state:any){for(const p of [1,2]){const pl=player(state,p);for(const c of pl.champions){c.damage=0;c.tempPow=0;if(!c.defeated)c.tapped=false}pl.fireCloud=false;pl.lyrandelUsed=false;pl.firstMagicCast=false;pl.deck=shuffle([...pl.deck,...pl.hand]);pl.hand=[];pl.selected=false}for(const m of state.board.monsters){m.damage=0;m.tempPow=0}state.turn+=1;state.status='select';state.focus=state.turn%2===1?1:2;state.priority=null;state.mainPasses=0;state.priorityPasses=0;state.stack=[];state.combat=null;state.pendingChoice=null;log(state,`Fine turno. Scegliete 6 carte per il turno ${state.turn}.`)}
function finishAction(state:any,initiator:number){state.priority=null;state.priorityPasses=0;state.stack=[];state.stackInitiator=null;state.combat=null;state.mainPasses=0;state.focus=other(initiator)}
function resolveCombat(state:any){const c=state.combat;if(!c)return;const atk=champById(state,c.attacker.player,c.attacker.champId);if(!atk||atk.defeated||c.cancelled){log(state,'Il combattimento viene annullato.');return}const amount=currentPow(state,c.attacker.player,atk);if(c.target.type==='champion'){const target=champById(state,c.target.player,c.target.champId);if(target&&!target.defeated){log(state,`${atk.name} attacca ${target.name} con ${amount} POW.`);damageChampion(state,c.target.player,target.id,amount,atk.name)}else log(state,'Il bersaglio del combattimento non è più valido.')}else{const m=monsterByUid(state,c.target.uid);if(m){log(state,`${atk.name} attacca ${MONSTER_DEFS[m.cardId].name} con ${amount} POW.`);damageMonster(state,c.attacker.player,m.uid,amount,atk.name,true)}else log(state,'Il Mostro bersaglio non è più sul campo: l’attacco fallisce.')}}
function validateTargetEnemy(state:any,p:number,t:any){if(t?.type==='champion')return t.player===other(p)&&!!champById(state,t.player,t.champId)&&!champById(state,t.player,t.champId).defeated;if(t?.type==='monster')return canTargetMonster(state,t.uid);return false}
function validateChosenMonster(state:any,uid:string){return canTargetMonster(state,uid)}
function resolveSpell(state:any,item:any){const p=item.actor,pl=player(state,p),card=CARD_DEFS[item.cardId],t=item.targets||{};log(state,`Si risolve ${card.name}.`);switch(card.effect){case'taglio':if(state.combat){const ac=state.combat.attacker;damageChampion(state,ac.player,ac.champId,spellDamage(state,p,1,item),card.name)}break;case'sfera':{const dmg=spellDamage(state,p,1,item);if(t.enemy?.type==='champion')damageChampion(state,t.enemy.player,t.enemy.champId,dmg,card.name);else if(t.enemy?.type==='monster')damageMonster(state,p,t.enemy.uid,dmg,card.name,true);damageChampion(state,p,t.ownChamp,dmg,card.name);break}case'corazza':{damageChampion(state,p,t.ownChamp,spellDamage(state,p,1,item),card.name);const c=champById(state,p,t.ownChamp);if(c&&!c.defeated){c.tempPow+=1;log(state,`${c.name} ottiene +1 POW fino alla fine del turno.`)}break}case'occhio':{if(pl.deck.length>=3){const burned=pl.deck.splice(0,3);pl.grave.push(...burned);const c=champById(state,p,t.ownChamp);if(c&&!c.defeated){c.tempPow+=2;log(state,`${pl.name} manda 3 carte nel Cimitero. ${c.name} ottiene +2 POW.`)}}break}case'mano':{const n=pl.hand.length;pl.grave.push(...pl.hand);pl.hand=[];const c=champById(state,p,t.ownChamp);if(c&&!c.defeated){c.tempPow+=n;log(state,`${pl.name} scarta ${n} carte. ${c.name} ottiene +${n} POW.`)}break}case'nube':pl.fireCloud=true;log(state,`Le Magie di ${pl.name} infliggono +1 danno fino alla fine del turno.`);break;case'tornado':{const dmg=spellDamage(state,p,1,item);for(const q of [1,2])for(const c of player(state,q).champions.filter((x:any)=>!x.defeated))damageChampion(state,q,c.id,dmg,card.name);for(const m of [...state.board.monsters])if(monsterByUid(state,m.uid))damageMonster(state,p,m.uid,dmg,card.name,true);break}case'fendente':{const dmg=spellDamage(state,p,3,item);if(t.enemy?.type==='champion')damageChampion(state,t.enemy.player,t.enemy.champId,dmg,card.name);else if(t.enemy?.type==='monster')damageMonster(state,p,t.enemy.uid,dmg,card.name,true);break}case'berserk':{const r=damageChampion(state,p,t.ownChamp,spellDamage(state,p,1,item),card.name);const c=champById(state,p,t.ownChamp);if(r.wounded&&c&&!c.defeated){c.tempPow+=5;c.tapped=false;log(state,`${c.name} entra in Berserk: +5 POW e diventa attivo.`)}break}case'stupido':damageMonster(state,p,t.monsterUid,spellDamage(state,p,1,item),card.name,true);break;case'riflesso':{const c=champById(state,p,t.ownChamp);if(c&&!c.defeated)damageMonster(state,p,t.monsterUid,spellDamage(state,p,c.damage,item),card.name,true);break}case'festa':{const top=pl.monsterDeck.slice(0,3);state.pendingChoice={type:'festa',player:p,top};log(state,`${pl.name} guarda le prime ${top.length} carte del proprio Monster Deck.`);return'pause'}case'taglio_ninjitsu':damageMonster(state,p,t.monsterUid,spellDamage(state,p,2,item),card.name,true);break;case'katana':{const c=champById(state,p,t.ownChamp);if(c&&!c.defeated){c.tapped=false;log(state,`${c.name} diventa attivo.`)}break}case'marea':{if(state.combat&&state.combat.attacker&&state.combat.target){const involved=(state.combat.attacker.player===p&&state.combat.attacker.champId===t.ownChamp)||(state.combat.target.type==='champion'&&state.combat.target.player===p&&state.combat.target.champId===t.ownChamp);if(involved){state.combat.cancelled=true;log(state,'Alta Marea annulla il combattimento.')}}break}case'albero':{const c=champById(state,p,t.ownChamp);if(c&&!c.defeated){c.tempPow+=2;log(state,`${c.name} ottiene +2 POW fino alla fine del turno.`)}break}case'sguardo':{const m=monsterByUid(state,t.monsterUid);if(m)killMonster(state,p,m,card.name);break}case'mille':{const c=champById(state,p,t.ownChamp);if(c&&!c.defeated){c.tempPow+=4;log(state,`${c.name} ottiene +4 POW fino alla fine del turno.`)}break}}return'ok'}
function continueResolution(state:any){while(state.stack.length){const item=state.stack.pop();const r=resolveSpell(state,item);player(state,item.actor).grave.push(item.cardId);if(r==='pause')return}const initiator=state.stackInitiator??state.combat?.initiator??state.focus;if(state.combat)resolveCombat(state);finishAction(state,initiator)}
export function act(state:any,p:number,a:any){if(state.status==='gameover')throw new Error('La partita è terminata.');if(state.pendingChoice){if(a.type!=='resolve_choice'||state.pendingChoice.player!==p)throw new Error('C’è una scelta in attesa.');if(state.pendingChoice.type==='festa'){const pl=player(state,p),top=state.pendingChoice.top;const discard=(a.discard||[]).filter((id:string)=>top.includes(id));const remain=top.filter((id:string)=>!discard.includes(id));const order=(a.order||[]);if(order.length!==remain.length||order.some((id:string)=>!remain.includes(id)))throw new Error('Ordine non valido.');pl.monsterDeck=[...order,...pl.monsterDeck.slice(top.length)];pl.monsterGrave.push(...discard);state.pendingChoice=null;log(state,`${pl.name} scarta ${discard.length} Mostr${discard.length===1?'o':'i'} e riordina gli altri.`);continueResolution(state);return state}}if(a.type==='select_cards'){if(state.status!=='select')throw new Error('Non è il momento di scegliere le carte.');const pl=player(state,p);if(pl.selected)throw new Error('Hai già confermato.');const ids=[...new Set(a.cardIds||[])];if(ids.length!==6)throw new Error('Devi scegliere esattamente 6 carte.');if(ids.some((id:string)=>!pl.deck.includes(id)))throw new Error('Carta non disponibile nel mazzo.');pl.hand=ids;pl.deck=pl.deck.filter((id:string)=>!ids.includes(id));pl.selected=true;log(state,`${pl.name} ha scelto le 6 carte.`);startMainIfReady(state);return state}if(state.status!=='main')throw new Error('La fase principale non è attiva.');if(a.type==='attack'){if(state.priority||state.stack.length||state.combat)throw new Error('Non puoi attaccare durante una Catena.');if(state.focus!==p)throw new Error('Non hai il Focus.');const c=champById(state,p,a.champId);if(!c||c.defeated||c.tapped)throw new Error('Campione non disponibile.');if(!validateTargetEnemy(state,p,a.target))throw new Error('Bersaglio non valido.');const sal=monsterCount(state,'salamandra_vulcanica');if(sal>0){c.tempPow=(c.tempPow||0)-sal;log(state,`${c.name} perde ${sal} POW per la Salamandra Vulcanica fino alla fine del turno.`);if(c.damage>=currentPow(state,p,c)&&!c.defeated)woundChampion(state,p,c,'Salamandra Vulcanica')}if(c.defeated)throw new Error('Il Campione viene sconfitto prima di poter attaccare.');c.tapped=true;state.combat={initiator:p,attacker:{player:p,champId:c.id},target:a.target,cancelled:false};state.priority=other(p);state.priorityPasses=0;state.mainPasses=0;log(state,`${player(state,p).name} dichiara un attacco con ${c.name}.`);return state}if(a.type==='cast'){const pl=player(state,p),card=CARD_DEFS[a.cardId];if(!card||!pl.hand.includes(card.id))throw new Error('Carta non disponibile in mano.');if(state.pendingChoice)throw new Error('Scelta in attesa.');if(state.stack.length){if(state.priority!==p)throw new Error('Non hai priorità nella Catena.');if(card.speed!=='instant')throw new Error('Durante una Catena puoi giocare solo Istantanee.')}else if(state.combat){if(state.priority!==p)throw new Error('Non hai priorità nel combattimento.');if(card.speed!=='response')throw new Error('La prima carta durante un combattimento deve essere una Risposta.')}else if(state.focus!==p)throw new Error('Non hai il Focus.');if(card.effect==='taglio'&&!state.combat)throw new Error('Taglio Fiammante richiede un Campione attaccante.');if(card.effect==='marea'&&!state.combat)throw new Error('Alta Marea richiede un combattimento.');if(card.effect==='occhio'&&pl.deck.length<3)throw new Error('Non hai 3 carte nel mazzo da scartare.');if(['stupido','taglio_ninjitsu','sguardo','riflesso'].includes(card.effect)&&!validateChosenMonster(state,a.targets?.monsterUid))throw new Error('Devi scegliere il Guardiano della Foresta finché è in gioco.');if(['sfera','fendente'].includes(card.effect)&&a.targets?.enemy?.type==='monster'&&!validateChosenMonster(state,a.targets.enemy.uid))throw new Error('Devi scegliere il Guardiano della Foresta finché è in gioco.');if(!canPay(state,p,card))throw new Error('Anime insufficienti.');const cost=pay(state,p,card);pl.hand=pl.hand.filter((id:string)=>id!==card.id);let golemBoost=0;if(card.type==='Magia'){if(pl.firstMagicCast===undefined)pl.firstMagicCast=false;if(!pl.firstMagicCast){pl.firstMagicCast=true;golemBoost=monsterCount(state,'golem_magmatico')}}state.stack.push({actor:p,cardId:card.id,targets:a.targets||{},golemBoost});if(state.stack.length===1)state.stackInitiator=state.combat?.initiator??p;state.mainPasses=0;state.priorityPasses=0;state.priority=other(p);log(state,`${pl.name} gioca ${card.name}${cost?` pagando ${cost} Anima${cost===1?'':'e'} ${card.color==='red'?'Rossa':'Verde'}`:''}${golemBoost?` (+${golemBoost} danno dal Golem Magmatico)`:''}.`);return state}if(a.type==='revive_phoenix'){if(state.priority||state.stack.length||state.combat||state.focus!==p)throw new Error('Puoi rianimare la Fenice solo con il Focus e senza Catene.');const pl=player(state,p),idx=pl.monsterGrave.indexOf('fenice_cremisi');if(idx<0)throw new Error('Fenice Cremisi non è nel tuo Cimitero Mostri.');const color=a.color==='red'?'red':a.color==='green'?'green':null;if(!color||pl.souls[color]<1)throw new Error('Devi pagare 1 Anima disponibile.');pl.souls[color]-=1;pl.monsterGrave.splice(idx,1);const m={uid:crypto.randomUUID(),cardId:'fenice_cremisi',owner:p,damage:0,tempPow:0};state.board.monsters.push(m);log(state,`${pl.name} paga 1 Anima ${color==='red'?'Rossa':'Verde'} e gioca Fenice Cremisi dal Cimitero.`);resolveMonsterEnter(state,m);state.focus=other(p);state.mainPasses=0;return state}if(a.type==='pass_priority'){if(state.priority!==p)throw new Error('Non hai priorità.');state.priorityPasses+=1;if(state.priorityPasses>=2){state.priorityPasses=0;if(state.stack.length)continueResolution(state);else if(state.combat){const init=state.combat.initiator;resolveCombat(state);finishAction(state,init)}}else state.priority=other(p);return state}if(a.type==='pass'){if(state.priority||state.stack.length||state.combat)throw new Error('Usa Passa priorità durante combattimenti o Catene.');if(state.focus!==p)throw new Error('Non hai il Focus.');state.mainPasses+=1;log(state,`${player(state,p).name} passa.`);if(state.mainPasses>=2)endTurn(state);else state.focus=other(p);return state}if(a.type==='recycle'){if(state.priority||state.stack.length||state.combat||state.focus!==p)throw new Error('Puoi Riciclare solo con il Focus e senza Catene.');const pl=player(state,p),cost=pl.recycleCount+1;const r=Number(a.red||0),g=Number(a.green||0);if(r+g!==cost||r<0||g<0||r>pl.souls.red||g>pl.souls.green)throw new Error('Pagamento del Riciclo non valido.');if(!pl.grave.length)throw new Error('Il Cimitero è vuoto.');pl.souls.red-=r;pl.souls.green-=g;pl.deck=shuffle([...pl.deck,...pl.grave]);pl.grave=[];pl.recycleCount+=1;log(state,`${pl.name} Ricicla il Cimitero pagando ${cost} Anime.`);state.focus=other(p);state.mainPasses=0;return state}throw new Error('Azione sconosciuta.')}
export function publicView(state:any,p:number){const s=clone(state);for(const q of [1,2]){const pl=s.players[String(q)];if(!pl)continue;pl.champions=pl.champions.map((c:any)=>({...c,pow:currentPow(state,q,player(state,q).champions.find((x:any)=>x.id===c.id))}));if(q!==p){pl.handCount=pl.hand.length;delete pl.hand;pl.deckCount=pl.deck.length;delete pl.deck;pl.graveCount=pl.grave.length;delete pl.grave;pl.monsterDeckCount=pl.monsterDeck.length;delete pl.monsterDeck;pl.monsterGraveCount=pl.monsterGrave.length;delete pl.monsterGrave}else{pl.handCards=pl.hand.map((id:string)=>({...CARD_DEFS[id],effectiveCost:dynamicCost(state,q,CARD_DEFS[id])}));pl.deckCards=pl.deck.map((id:string)=>CARD_DEFS[id]);pl.graveCards=pl.grave.map((id:string)=>CARD_DEFS[id])}}s.board.monsters=s.board.monsters.map((m:any)=>({...m,...MONSTER_DEFS[m.cardId],pow:currentMonsterPow(state,m)}));s.cardDefs=CARD_DEFS;s.monsterDefs=MONSTER_DEFS;s.you=p;if(s.pendingChoice&&s.pendingChoice.player!==p)s.pendingChoice={type:s.pendingChoice.type,player:s.pendingChoice.player,hidden:true};return s}
