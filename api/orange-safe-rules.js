export default async function handler(req,res){try{const r=await fetch('https://gmunayvayjzzyrigaesx.supabase.co/functions/v1/soulforge-orange-safe-rules-test',{cache:'no-store'});const text=await r.text();res.status(r.status).setHeader('content-type',r.headers.get('content-type')||'application/json').send(text)}catch(e){res.status(500).json({ok:false,error:e instanceof Error?e.message:String(e)})}}
